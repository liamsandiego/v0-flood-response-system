# =============================================================================
# RapidRelay Backend — Python Dependencies
# =============================================================================

# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0

# Database (async SQLite)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# ML model serving
joblib>=1.4.0
xgboost>=2.0.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# Geospatial (for STAC catalog queries)
pystac-client>=0.8.0

# Supabase (cloud database + auth)
supabase>=2.0.0

# AI interpretation (Groq — fast LLM inference)
groq>=0.9.0
